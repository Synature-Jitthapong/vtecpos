package com.vtec.j1tth4.vtecpos.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.vtec.j1tth4.vtecpos.dao.BankName;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table BANK_NAME.
*/
public class BankNameDao extends AbstractDao<BankName, Integer> {

    public static final String TABLENAME = "BANK_NAME";

    /**
     * Properties of entity BankName.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property BankNameID = new Property(0, int.class, "BankNameID", true, "BANK_NAME_ID");
        public final static Property BankName = new Property(1, String.class, "BankName", false, "BANK_NAME");
        public final static Property Deleted = new Property(2, int.class, "Deleted", false, "DELETED");
    };


    public BankNameDao(DaoConfig config) {
        super(config);
    }
    
    public BankNameDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'BANK_NAME' (" + //
                "'BANK_NAME_ID' INTEGER PRIMARY KEY NOT NULL ," + // 0: BankNameID
                "'BANK_NAME' TEXT NOT NULL ," + // 1: BankName
                "'DELETED' INTEGER NOT NULL );"); // 2: Deleted
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'BANK_NAME'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, BankName entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getBankNameID());
        stmt.bindString(2, entity.getBankName());
        stmt.bindLong(3, entity.getDeleted());
    }

    /** @inheritdoc */
    @Override
    public Integer readKey(Cursor cursor, int offset) {
        return cursor.getInt(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public BankName readEntity(Cursor cursor, int offset) {
        BankName entity = new BankName( //
            cursor.getInt(offset + 0), // BankNameID
            cursor.getString(offset + 1), // BankName
            cursor.getInt(offset + 2) // Deleted
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, BankName entity, int offset) {
        entity.setBankNameID(cursor.getInt(offset + 0));
        entity.setBankName(cursor.getString(offset + 1));
        entity.setDeleted(cursor.getInt(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected Integer updateKeyAfterInsert(BankName entity, long rowId) {
        return entity.getBankNameID();
    }
    
    /** @inheritdoc */
    @Override
    public Integer getKey(BankName entity) {
        if(entity != null) {
            return entity.getBankNameID();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
